- rewrite g_entity_flags_string to show ALL fields
- resource database
+ split source code into subdirs (gl, game, sound, video, sys...) and move all to "src" directory
+ use plugins style for different entity renderers and sound formats
+ traceback on lua error
+ port engine to iphone
+ get rid of dlopen, use linking instead
+ multiple shapes for set_body to allow more complex shapes for entity
+ -nograb option
